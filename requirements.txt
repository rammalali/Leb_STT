# Web server
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.9.2

# ASR (Whisper)
# NOTE: install torch + torchvision from the matching CUDA channel BEFORE this
# file, otherwise pip will pull the CPU build from PyPI, e.g.
#   pip install --index-url https://download.pytorch.org/whl/cu126 torch torchvision
transformers>=4.46.0
accelerate>=1.0.0

# Audio decoding
librosa>=0.10.2
soundfile>=0.12.1

# Speaker diarization (open, no gated models)
speechbrain>=1.0.0
scikit-learn>=1.3.0
